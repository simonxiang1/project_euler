/* Problem 4 */
/* Largest palindome that's the product of two three digit numbers */
/* Compile with gcc problem4.c -lm on a unix based system */
#include <stdio.h>
#include <math.h>
#include "config.h"

int main () {
    c1 = pow(10, digit) - 1;
    c2 = pow(10, digit) - 1;
    to = pow(10, digit - 1) - 1;
    printf("c1: %g c2: %g to: %g \n", c1, c2, to);
    
    return 0;
}
